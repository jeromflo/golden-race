export interface IApplication {
    selectedBalls: number[],
    coloursButtons: string[],
    amountPay: number,
    amountUsed: number,
    MAXUSED: number,
    MINUSED: number,
    randomWins: number,
}